#include<stdio.h>
int main()
{
	int n, i, fact;
	fact=i=1;
	printf("Enter factorial of a number:");
	scanf("%d", &n);
	while(i<=n)
	{
		fact*=i;
		i++;
	}
	printf("The factorial of %d is: %d", n, fact);
	return 0;
}
